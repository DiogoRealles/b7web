const sum2 = (x: number, y: number) => {
  return x + y;
};

const sub2 = (x: number, y: number) => {
  return x - y;
};

const mult2 = (x: number, y: number) => {
  return x * y;
};

export default {
  sum2,
  sub2,
  mult2,
};
